clc
clear all
close all


%% LOAD DATA

try
    load('Data\Descarga-Carga.mat')
catch
    disp('No se ha creado el archivo de datos')
    return
end


%% 
% k = [];
% for i = 1:length(Descarga)
%     k{i} = - ( log(Descarga(i).t) - log(-Descarga(i).It) )./log(Descarga(i).I);
% end

k = ( log(Descarga(i).It(end)) - ) )


N_Serie = round(Descarga(1).V(1)/4.);